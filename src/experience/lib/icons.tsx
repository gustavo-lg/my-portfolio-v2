import { Boxes, Layers, User, Mail, type LucideIcon } from "lucide-react";
import type { IconName } from "@/content/types";

export const categoryIcons: Record<IconName, LucideIcon> = {
  Boxes,
  Layers,
  User,
  Mail,
};
