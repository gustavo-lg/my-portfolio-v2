/**
 * How long the page-in / page-out keyframes (tailwind.config.ts) run. The
 * outgoing layer is kept mounted for this long, and nav controls stay locked
 * for the same window to swallow double-clicks.
 */
export const SWAP_MS = 1400;
